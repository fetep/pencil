<% if @select_hosts.size == 0 %> [no hosts]<% else %>
<select onchange="switchToHost(this.options[this.selectedIndex].value)">
  <option value="">-select host-</option><% @select_hosts.each do |h|
                 option = "#{h.name}"
                 value = "#{h.name}" %>
  <option value="<%= value %>"><%= option %></option><% end %>
</select>
<% end %>